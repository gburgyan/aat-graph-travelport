# Travel agency addon.
# Add travel agency details to the reservation within an existing workbench.

intent:
  description: "Add travel agency details to the reservation"

execution:
  steps:
    - node: addTravelAgency
      description: "Add travel agency information"
      assertions:
        mechanical:
          - type: status
            expect: 201
      values:
        workbenchId: AUTOWIRE
